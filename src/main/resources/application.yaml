spring:
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      batch-size: 100
      concurrency: 2
      enable-auto-commit: false
      auto-offset-reset: earliest
    producer:
      max-block-ms: 200
  data:
    redis:
      host: localhost
      port: 6379